# ============================================================
# RAG 教程依赖清单（按课程逐步启用）
# 安装方式：pip install -r requirements.txt
# ============================================================

# ---- v0.1 关键词检索（标准库即可，无需任何第三方依赖）----
# 本课无需安装任何包

# ---- v0.2 Embedding（第 2 课已启用） ----
sentence-transformers>=2.7.0
numpy>=1.26.0

# ---- v0.3 接入 MiMo LLM（第 3 课已启用） ----
openai>=1.30.0
python-dotenv>=1.0.0

# ---- v0.4 文档加载与切分（第 4 课启用） ----
# pypdf>=4.0.0
# python-docx>=1.1.0
# beautifulsoup4>=4.12.0
# markdown-it-py>=3.0.0

# ---- v0.5 向量库（第 5 课启用） ----
# chromadb>=0.5.0

# ---- v0.6 混合检索（第 6 课启用） ----
# rank-bm25>=0.2.2
# FlagEmbedding>=1.2.0     # bge-reranker

# ---- v0.9 评估（第 9 课启用） ----
# ragas>=0.1.10
# datasets>=2.18.0

# ---- v1.0 服务化（第 10 课启用） ----
# fastapi>=0.110.0
# uvicorn[standard]>=0.29.0
# streamlit>=1.34.0

# ============================================================
# 站点构建（jupyter-book v2 / mystmd）
# 用 .venv 跑 `jupyter-book start` 本地预览课程站
# ============================================================
jupyter-book>=2.1.0
myst-nb>=1.1.0
jupytext>=1.16.0
